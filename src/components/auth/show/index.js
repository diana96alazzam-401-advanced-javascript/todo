const Show = (props) => (props.condition ? props.children : null);
export default Show;

// export default (props) => (props.condition ? props.children : null);

// export const Show = (props) => (props.condition ? props.children : null);
